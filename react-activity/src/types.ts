import * as React from "react";

export interface ReactActivityIndicatorProps {
  style?: React.CSSProperties;
  color?: string;
  animating?: boolean;
  speed?: number;
  size?: number;
  className?: string;
}
